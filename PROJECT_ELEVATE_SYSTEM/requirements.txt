# PROJECT ELEVATE (Bulletproof Enterprise Edition) — United Brothers Co.
# Python 3.10+
#
# NOTE: Streamlit Community Cloud installs from THIS file (it sits next to the
# app entry point dashboard.py), so it must list every dashboard dependency.
pandas>=2.0
openpyxl>=3.1
tabulate>=0.9          # pretty Markdown tables for DataFrame.to_markdown()
streamlit>=1.42        # web dashboard (dashboard.py); >=1.42 for st.login/st.user OIDC auth
plotly>=5.24           # interactive charts on the dashboard
psycopg[binary]>=3.1   # durable Postgres/Supabase backend (portfolio_store);
                       # only imported when ELEVATE_DATABASE_URL is configured

# Optional AI parsing (boq_auditor / site_tracker) — install separately:
#   pip install -r requirements-optional.txt
# Development / testing:
#   pip install -r requirements-dev.txt
