# PROJECT ELEVATE (Bulletproof Enterprise Edition) — United Brothers Co.
# Python 3.10+
pandas>=2.0
openpyxl>=3.1
tabulate>=0.9          # pretty Markdown tables for DataFrame.to_markdown()
anthropic>=0.40        # optional — AI parsing (boq_auditor / site_tracker)
